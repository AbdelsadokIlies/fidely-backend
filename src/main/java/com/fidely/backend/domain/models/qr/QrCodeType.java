package com.fidely.backend.domain.models.qr;

/**
 * Définit les différents types de QR codes utilisés par la plateforme.
 */
public enum QrCodeType {
    MAIN,
    LOYALTY,
    PROMOTION,
    REWARD,
    WHEEL,
    TICKET
}